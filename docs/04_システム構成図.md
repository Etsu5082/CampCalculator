# 合宿費用計算アプリ システム構成図

## 1. システム全体構成

```
┌─────────────────────────────────────────────────────────────────┐
│                        クライアント                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐            │
│  │    PC       │  │  タブレット  │  │ スマートフォン│            │
│  │  (Chrome)   │  │  (Safari)   │  │   (Chrome)   │            │
│  └──────┬──────┘  └──────┬──────┘  └──────┬──────┘            │
└─────────┼────────────────┼────────────────┼────────────────────┘
          │                │                │
          └────────────────┼────────────────┘
                           │ HTTPS
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                  お名前.com RSプラン                             │
│  ┌───────────────────────────────────────────────────────────┐ │
│  │                    Apache Web Server                       │ │
│  │  ┌─────────────────────────────────────────────────────┐  │ │
│  │  │                     PHP 7.4+                         │  │ │
│  │  │  ┌───────────────────────────────────────────────┐  │  │ │
│  │  │  │              アプリケーション                   │  │  │ │
│  │  │  │  ┌─────────┐ ┌─────────┐ ┌─────────────────┐ │  │  │ │
│  │  │  │  │ Router  │ │Controller│ │     Model       │ │  │  │ │
│  │  │  │  └────┬────┘ └────┬────┘ └────────┬────────┘ │  │  │ │
│  │  │  │       │           │                │          │  │  │ │
│  │  │  │       └───────────┴────────────────┘          │  │  │ │
│  │  │  └───────────────────────┬───────────────────────┘  │  │ │
│  │  └─────────────────────────┼───────────────────────────┘  │ │
│  └─────────────────────────────┼─────────────────────────────┘ │
│                                │                               │
│  ┌─────────────────────────────▼─────────────────────────────┐ │
│  │                      MySQL 5.7                            │ │
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────────────┐ │ │
│  │  │  camps  │ │time_slots│ │participants│ │   expenses   │ │ │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────────────┘ │ │
│  └───────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. ディレクトリ構成

```
/public_html/
└── camp-calc/                    # アプリケーションルート
    │
    ├── index.php                 # エントリーポイント
    ├── .htaccess                 # URL書き換え設定
    │
    ├── config/
    │   ├── database.php          # DB接続設定
    │   └── app.php               # アプリ設定
    │
    ├── src/
    │   ├── Controllers/
    │   │   ├── AuthController.php
    │   │   ├── CampController.php
    │   │   ├── ParticipantController.php
    │   │   ├── TimeSlotController.php
    │   │   ├── ExpenseController.php
    │   │   ├── CalculationController.php
    │   │   ├── MemberController.php       # 会員名簿管理
    │   │   ├── EnrollmentController.php   # 入会フォーム
    │   │   ├── ApplicationController.php  # 合宿申し込み
    │   │   └── ChangeRequestController.php # 変更リクエスト
    │   │
    │   ├── Models/
    │   │   ├── Camp.php
    │   │   ├── Participant.php
    │   │   ├── TimeSlot.php
    │   │   ├── ParticipantSlot.php
    │   │   ├── MealAdjustment.php
    │   │   ├── Expense.php
    │   │   ├── Member.php                 # 会員名簿
    │   │   ├── CampToken.php              # 申し込みURLトークン
    │   │   ├── CampApplication.php        # 合宿申し込み
    │   │   ├── ChangeRequest.php          # 変更リクエスト
    │   │   └── EmailLog.php               # メール送信ログ
    │   │
    │   ├── Services/
    │   │   ├── CalculationService.php     # 費用計算ロジック
    │   │   ├── ExportService.php          # PDF/Excel出力
    │   │   ├── StudentIdParserService.php # 学籍番号解析
    │   │   ├── MemberImportService.php    # 会員インポート
    │   │   ├── GradeUpdateService.php     # 学年更新処理
    │   │   └── EmailService.php           # メール送信
    │   │
    │   └── Core/
    │       ├── Database.php             # PDO wrapper
    │       ├── Router.php               # ルーティング
    │       ├── Session.php              # セッション管理
    │       └── Response.php             # レスポンス処理
    │
    ├── views/
    │   ├── layouts/
    │   │   ├── main.php                 # 共通レイアウト（管理者）
    │   │   └── public.php               # 共通レイアウト（公開ページ）
    │   │
    │   ├── auth/
    │   │   └── login.php
    │   │
    │   ├── camps/
    │   │   ├── index.php                # 一覧
    │   │   ├── create.php               # 新規作成
    │   │   └── detail.php               # 詳細（タブ）
    │   │
    │   ├── participants/
    │   │   └── _form.php                # 編集フォーム（モーダル）
    │   │
    │   ├── expenses/
    │   │   └── _form.php                # 編集フォーム（モーダル）
    │   │
    │   ├── results/
    │   │   ├── show.php                 # 計算結果
    │   │   └── partial-schedule.php     # 途参途抜一覧
    │   │
    │   ├── members/                     # 会員名簿管理（管理者）
    │   │   ├── index.php                # 会員一覧
    │   │   ├── _form.php                # 編集フォーム（モーダル）
    │   │   ├── import.php               # インポート画面
    │   │   ├── pending.php              # 入会申請一覧
    │   │   └── grade-update.php         # 学年更新画面
    │   │
    │   ├── applications/                # 申し込み管理（管理者）
    │   │   ├── index.php                # 申し込み一覧
    │   │   ├── token.php                # URL管理
    │   │   └── change-requests.php      # 変更リクエスト一覧
    │   │
    │   ├── enroll/                      # 入会フォーム（公開）
    │   │   ├── form.php                 # 入力フォーム
    │   │   ├── confirm.php              # 確認画面
    │   │   └── complete.php             # 完了画面
    │   │
    │   ├── apply/                       # 合宿申し込み（公開）
    │   │   ├── search.php               # 会員検索
    │   │   ├── confirm-info.php         # 情報確認
    │   │   ├── schedule.php             # 日程選択
    │   │   ├── review.php               # 最終確認
    │   │   └── complete.php             # 完了画面
    │   │
    │   └── guide.php                    # 使い方ガイド
    │
    ├── public/
    │   ├── css/
    │   │   └── style.css
    │   │
    │   └── js/
    │       ├── app.js                   # メインJS
    │       ├── participants.js          # 参加者管理
    │       └── calculation.js           # 計算表示
    │
    └── vendor/                          # ライブラリ（必要に応じて）
        └── ...
```

---

## 3. 技術スタック

### 3.1 サーバーサイド

| 項目 | 技術 | バージョン |
|------|------|------------|
| 言語 | PHP | 7.4以上 |
| データベース | MySQL | 5.7 |
| Webサーバー | Apache | RSプラン標準 |

### 3.2 フロントエンド

| 項目 | 技術 | 備考 |
|------|------|------|
| HTML | HTML5 | |
| CSS | CSS3 + Bootstrap 5 | レスポンシブ対応 |
| JavaScript | Vanilla JS | jQuery不使用（軽量化） |
| PDF出力 | TCPDF または mPDF | PHP製ライブラリ |
| Excel出力 | PhpSpreadsheet | PHP製ライブラリ |

---

## 4. API設計

### 4.1 エンドポイント一覧

| メソッド | パス | 説明 |
|----------|------|------|
| POST | /api/auth/login | ログイン |
| POST | /api/auth/logout | ログアウト |
| GET | /api/camps | 合宿一覧取得 |
| POST | /api/camps | 合宿新規作成 |
| GET | /api/camps/{id} | 合宿詳細取得 |
| PUT | /api/camps/{id} | 合宿更新 |
| DELETE | /api/camps/{id} | 合宿削除 |
| POST | /api/camps/{id}/duplicate | **合宿複製**（日程・費用設定をコピー） |
| GET | /api/camps/{id}/time-slots | タイムスロット取得 |
| PUT | /api/camps/{id}/time-slots | タイムスロット一括更新 |
| GET | /api/camps/{id}/participants | 参加者一覧取得 |
| POST | /api/camps/{id}/participants | 参加者追加 |
| POST | /api/camps/{id}/participants/import | **参加者CSV一括登録** |
| PUT | /api/participants/{id} | 参加者更新 |
| DELETE | /api/participants/{id} | 参加者削除 |
| GET | /api/camps/{id}/expenses | 雑費一覧取得 |
| POST | /api/camps/{id}/expenses | 雑費追加 |
| PUT | /api/expenses/{id} | 雑費更新 |
| DELETE | /api/expenses/{id} | 雑費削除 |
| GET | /api/camps/{id}/calculate | 費用計算実行 |
| GET | /api/camps/{id}/partial-schedule | **途参途抜スケジュール取得** |
| GET | /api/camps/{id}/export/pdf | PDF出力 |
| GET | /api/camps/{id}/export/excel | Excel出力 |
| POST | /pdf/upload | **PDF契約書アップロード・解析** |
| POST | /pdf/apply | **解析結果を合宿に反映** |
| GET | /api/members | **会員一覧取得** |
| POST | /api/members | **会員追加** |
| GET | /api/members/{id} | **会員詳細取得** |
| PUT | /api/members/{id} | **会員更新** |
| DELETE | /api/members/{id} | **会員削除** |
| POST | /api/members/import | **会員一括インポート** |
| GET | /api/members/pending | **承認待ち会員一覧** |
| POST | /api/members/{id}/approve | **会員承認** |
| POST | /api/members/{id}/reject | **会員却下** |
| POST | /api/members/grade-update | **学年一括更新** |
| GET | /api/camps/{id}/token | **申し込みURLトークン取得** |
| POST | /api/camps/{id}/token | **申し込みURLトークン発行** |
| PUT | /api/camps/{id}/token | **申し込みURLトークン更新** |
| GET | /api/camps/{id}/applications | **申し込み一覧取得** |
| GET | /api/change-requests | **変更リクエスト一覧取得** |
| POST | /api/change-requests/{id}/approve | **変更リクエスト承認** |
| POST | /api/change-requests/{id}/reject | **変更リクエスト却下** |

**ページルート（HTML表示）**

| メソッド | パス | 説明 |
|----------|------|------|
| GET | / | トップページ（リダイレクト） |
| GET | /login | ログイン画面 |
| GET | /camps | 合宿一覧（ダッシュボード） |
| GET | /camps/{id} | 合宿詳細画面 |
| GET | /camps/{id}/result | 計算結果画面 |
| GET | /camps/{id}/partial-schedule | 途参途抜一覧画面 |
| GET | /pdf/upload | **PDF契約書アップロード画面** |
| GET | /pdf/review | **PDF解析結果確認画面** |
| GET | /guide | **使い方ガイド画面** |
| GET | /members | **会員名簿一覧画面** |
| GET | /members/import | **会員インポート画面** |
| GET | /members/pending | **入会申請一覧画面** |
| GET | /members/grade-update | **学年更新画面** |
| GET | /camps/{id}/applications | **申し込み一覧画面** |
| GET | /camps/{id}/token | **申し込みURL管理画面** |
| GET | /change-requests | **変更リクエスト一覧画面** |
| GET | /enroll | **入会フォーム画面（公開）** |
| POST | /enroll | **入会申請送信** |
| GET | /apply/{token} | **合宿申し込みフォーム画面（公開）** |
| POST | /apply/{token} | **合宿申し込み送信** |
| GET | /apply/{token}/search | **会員検索API（申し込みフォーム用）** |

### 4.2 レスポンス形式

```json
// 成功時
{
  "success": true,
  "data": { ... }
}

// エラー時
{
  "success": false,
  "error": {
    "code": "VALIDATION_ERROR",
    "message": "入力内容に誤りがあります",
    "details": { ... }
  }
}
```

---

## 5. セキュリティ設計

### 5.1 認証・認可

```
┌─────────────────────────────────────────────────────────────┐
│                      認証フロー                              │
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐ │
│  │ログイン │───→│パスワード│───→│セッション│───→│ 認証済み │ │
│  │ フォーム │    │  照合   │    │  発行   │    │  状態   │ │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘ │
│                                                             │
│  - パスワードはハッシュ化して保存（password_hash）          │
│  - セッションは24時間で有効期限切れ                         │
│  - CSRFトークンで不正リクエスト防止                         │
└─────────────────────────────────────────────────────────────┘
```

### 5.2 入力検証

| 対策 | 実装方法 |
|------|----------|
| SQLインジェクション | PDO prepared statements |
| XSS | htmlspecialchars() でエスケープ |
| CSRF | セッショントークン検証 |

---

## 6. 計算ロジック詳細

### 6.1 処理フロー

```
┌─────────────────────────────────────────────────────────────┐
│                     費用計算フロー                           │
│                                                             │
│  ┌─────────┐    ┌─────────────┐    ┌─────────────────────┐ │
│  │合宿データ│───→│参加者ループ │───→│  個人費用計算        │ │
│  │  取得   │    │            │    │  - 宿泊費            │ │
│  └─────────┘    └─────────────┘    │  - 保険料            │ │
│                       │           │  - 宴会場            │ │
│                       │           │  - 食事調整          │ │
│                       │           └─────────────────────┘ │
│                       │                     │              │
│                       │                     ▼              │
│                       │           ┌─────────────────────┐ │
│                       │           │  割り勘費用計算      │ │
│                       │           │  - バス（往/復）     │ │
│                       │           │  - 高速（往/復）     │ │
│                       │           │  - 施設費（コマ別）  │ │
│                       │           │  - 雑費              │ │
│                       │           └─────────────────────┘ │
│                       │                     │              │
│                       │                     ▼              │
│                       │           ┌─────────────────────┐ │
│                       └──────────→│    合計算出          │ │
│                                   └─────────────────────┘ │
│                                             │              │
│                                             ▼              │
│                                   ┌─────────────────────┐ │
│                                   │   結果返却           │ │
│                                   └─────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

### 6.2 計算式

```php
// 参加者ごとの負担額計算（疑似コード）
function calculateParticipantFee($participant, $camp) {
    $total = 0;

    // 1. 宿泊費
    $nights = $participant->leave_day - $participant->join_day;
    $total += $camp->lodging_fee_per_night * $nights;

    // 2. 保険料（固定）
    $total += $camp->insurance_fee;

    // 3. 宴会場使用料
    foreach ($banquetSlots as $slot) {
        if ($participant->isAttending($slot)) {
            $attendees = countAttendees($slot);
            $total += $slot->facility_fee / $attendees;
        }
    }

    // 4. 食事調整
    $total += calculateMealAdjustment($participant, $camp);

    // 5. バス代（往復一括 or 往路/復路別）
    if ($camp->bus_fee_separate) {
        // 往路/復路別設定
        if ($participant->use_outbound_bus) {
            $outboundUsers = countOutboundBusUsers($camp);
            $total += $camp->bus_fee_outbound / $outboundUsers;
            $total += $camp->highway_fee_outbound / $outboundUsers;
        }
        if ($participant->use_return_bus) {
            $returnUsers = countReturnBusUsers($camp);
            $total += $camp->bus_fee_return / $returnUsers;
            $total += $camp->highway_fee_return / $returnUsers;
        }
    } else {
        // 往復一括設定
        if ($participant->use_outbound_bus && $participant->use_return_bus) {
            $roundTripUsers = countRoundTripBusUsers($camp);
            $total += $camp->bus_fee_round_trip / $roundTripUsers;
        } elseif ($participant->use_outbound_bus) {
            $outboundOnlyUsers = countOutboundOnlyUsers($camp);
            $total += ($camp->bus_fee_round_trip / 2) / $outboundOnlyUsers;
        } elseif ($participant->use_return_bus) {
            $returnOnlyUsers = countReturnOnlyUsers($camp);
            $total += ($camp->bus_fee_round_trip / 2) / $returnOnlyUsers;
        }
        // 高速代
        if ($participant->use_outbound_bus) {
            $total += $camp->highway_fee_outbound / countOutboundBusUsers($camp);
        }
        if ($participant->use_return_bus) {
            $total += $camp->highway_fee_return / countReturnBusUsers($camp);
        }
    }

    // 6. レンタカー（オプション有効時）
    if ($camp->use_rental_car && $participant->use_rental_car) {
        $rentalCarUsers = countRentalCarUsers($camp);
        $total += $camp->rental_car_fee / $rentalCarUsers;
        $total += $camp->rental_car_highway_fee / $rentalCarUsers;
    }

    // 7. 施設利用料（コマ別）
    foreach ($facilitySlots as $slot) {
        if ($participant->isAttending($slot)) {
            $attendees = countAttendees($slot);
            $total += $slot->facility_fee / $attendees;
        }
    }

    // 8. 雑費
    foreach ($expenses as $expense) {
        if ($expense->isTargetParticipant($participant)) {
            $targets = countExpenseTargets($expense);
            $total += $expense->amount / $targets;
        }
    }

    return round($total); // 端数処理
}
```

---

## 7. 開発・デプロイフロー

```
┌─────────────────────────────────────────────────────────────┐
│                    開発フロー                                │
│                                                             │
│  ┌─────────┐    ┌─────────┐    ┌─────────┐    ┌─────────┐ │
│  │ローカル │───→│  テスト │───→│  FTP    │───→│  本番   │ │
│  │  開発  │    │        │    │アップロード│    │ サーバー │ │
│  └─────────┘    └─────────┘    └─────────┘    └─────────┘ │
│                                                             │
│  ローカル環境: XAMPP / MAMP                                 │
│  デプロイ: FileZilla等でFTPアップロード                     │
└─────────────────────────────────────────────────────────────┘
```

---

## 8. 端数処理方針

| 項目 | 処理方法 |
|------|----------|
| 割り勘計算 | 四捨五入（round） |
| 端数の扱い | 最後の1人に調整分を加算 or 会計担当が負担 |
| 表示 | 円単位（小数点以下なし） |

---

## 9. PDF契約書読み取り機能

### 9.1 処理フロー

```
┌─────────────────────────────────────────────────────────────┐
│                   PDF解析処理フロー                          │
│                                                             │
│  ┌─────────────────┐                                       │
│  │ PDFアップロード │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ▼                                                 │
│  ┌─────────────────┐                                       │
│  │ ファイル検証   │                                       │
│  │ ・MIMEタイプ    │                                       │
│  │ ・サイズ（10MB）│                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ▼                                                 │
│  ┌─────────────────┐    ┌─────────────────┐               │
│  │ テキスト抽出   │───→│ テキストあり？  │               │
│  │（Smalto/PdfParser）│    └────────┬────────┘               │
│  └─────────────────┘              │                         │
│                          Yes ┌────┴────┐ No                 │
│                              ▼         ▼                    │
│                 ┌─────────────────┐ ┌─────────────────┐    │
│                 │ 契約書種別判定 │ │ AI-OCR解析      │    │
│                 │ ・毎日コムネット │ │（Claude Vision） │    │
│                 │ ・COSMO AGENCY  │ └────────┬────────┘    │
│                 └────────┬────────┘          │              │
│                          │                    │              │
│                          ▼                    │              │
│                 ┌─────────────────┐          │              │
│                 │ 正規表現で抽出 │          │              │
│                 └────────┬────────┘          │              │
│                          │                    │              │
│                          └──────────┬────────┘              │
│                                     │                        │
│                                     ▼                        │
│                          ┌─────────────────┐                │
│                          │ バリデーション │                │
│                          │ ・必須項目確認  │                │
│                          │ ・数値範囲確認  │                │
│                          └────────┬────────┘                │
│                                   │                          │
│                                   ▼                          │
│                          ┌─────────────────┐                │
│                          │ 確認・編集画面 │                │
│                          └────────┬────────┘                │
│                                   │                          │
│                                   ▼                          │
│                          ┌─────────────────┐                │
│                          │ DB反映        │                │
│                          │ campsテーブル  │                │
│                          └─────────────────┘                │
└─────────────────────────────────────────────────────────────┘
```

### 9.2 対応契約書フォーマット

| 契約書種別 | 判定キーワード | 抽出項目 |
|-----------|---------------|---------|
| 毎日コムネット「御予約確認書」 | 「御予約確認書」 | 宿泊費、入湯税、テニスコート料金、宴会場料金 |
| 毎日コムネット「貸切バス契約書」 | 「貸切バス」 | バス料金（往復） |
| 毎日コムネット「旅行申込書兼確定書」 | 「旅行申込書兼確定書」 | 総合契約（宿泊費、バス代、施設料金など） |
| COSMO AGENCY「貸切バス契約書」 | 「COSMO AGENCY」 | バス料金（往復） |
| その他（AI-OCR） | 判定できない場合 | Claude Vision APIで画像解析 |

### 9.3 使用ライブラリ

| ライブラリ | 用途 | バージョン |
|-----------|------|-----------|
| smalot/pdfparser | PDFテキスト抽出 | 2.11 |
| Claude Vision API | スキャンPDFのOCR | claude-sonnet-4-20250514 |

---

## 10. デフォルトスケジュール生成

### 10.1 タイムスロット自動生成ルール

```
┌─────────────────────────────────────────────────────────────┐
│                デフォルトスロット生成                        │
│                                                             │
│  1日目（初日）:                                              │
│    ・往路バス移動 → 午後練習 → 夕食 → 夜企画 → 宿泊       │
│    ※午前練習・昼食はなし（バス到着が午後のため）           │
│                                                             │
│  2日目〜N-1日目（中日）:                                     │
│    ・朝食 → 午前練習 → 昼食 → 午後練習 → 夕食 → 宿泊    │
│                                                             │
│  N日目（最終日）:                                            │
│    ・朝食 → 午前練習 → 昼食 → 復路バス移動                │
│    ※午後練習はなし（帰路のため）                           │
└─────────────────────────────────────────────────────────────┘
```

### 10.2 参加タイミング選択肢

| 日 | 選択可能なタイミング |
|----|---------------------|
| 1日目 | 往路バスから、午後イベントから、夕食から、夜から、宿泊から |
| 2日目以降 | 朝食から、午前イベントから、昼食から、午後イベントから、夕食から、夜から、宿泊から |

※1日目は午前練習・昼食がないため、朝食・午前・昼食の選択肢は表示しない

---

## 11. 学籍番号解析ロジック

### 11.1 学籍番号フォーマット

```
例: 1Y23F158-5
    │││ │└─ チェックディジット
    │││ └── 連番（3桁）
    │││└── 学科コード（創造・先進のみ）または学系コード（基幹のみ）
    ││└── 入学年度（下2桁）
    │└─┘
    └── 学部コード（2文字）
```

### 11.2 学部判定

| コード | 学部 |
|--------|------|
| 1W | 基幹理工学部 |
| 1X | 創造理工学部 |
| 1Y | 先進理工学部 |

### 11.3 学科判定（5文字目）

**創造理工学部（1X）**
| コード | 学科 |
|--------|------|
| A | 建築学科 |
| B | 総合機械工学科 |
| C | 経営システム工学科 |
| D | 社会環境工学科 |
| E | 環境資源工学科 |
| F | 社会文化領域 |

**先進理工学部（1Y）**
| コード | 学科 |
|--------|------|
| A | 物理学科 |
| B | 応用物理学科 |
| C | 化学・生命化学科 |
| D | 応用化学科 |
| E | 生命医科学科 |
| F | 電気・情報生命工学科 |

**基幹理工学部（1W）**
- 学科は学籍番号からは判定不可（進振り制度のため）
- 1年生は学系を選択
- 2年生以上は学科を選択

### 11.4 基幹理工学部の学系・学科

**2024年以前入学（旧学系）**
| 学系 | 進振り先学科 |
|------|-------------|
| 学系Ⅰ | 数学科、応用数理学科 |
| 学系Ⅱ | 応用数理学科、機械科学・航空宇宙学科、電子物理システム学科、情報理工学科、情報通信学科 |
| 学系Ⅲ | 情報理工学科、情報通信学科、表現工学科 |

**2025年以降入学（新学系）**
| 学系 | 進振り先学科 |
|------|-------------|
| 学系Ⅰ（数学系） | 数学科、応用数理学科 |
| 学系Ⅱ（工学系） | 応用数理学科、機械科学・航空宇宙学科、電子物理システム学科、情報通信学科 |
| 学系Ⅲ（情報系） | 応用数理学科、情報理工学科、情報通信学科 |
| 学系Ⅳ（メディア系） | 情報理工学科、情報通信学科、表現工学科 |

**基幹理工学部の全学科**
- 数学科
- 応用数理学科
- 機械科学・航空宇宙学科
- 電子物理システム学科
- 情報理工学科
- 情報通信学科
- 表現工学科

### 11.5 解析処理フロー

```
┌─────────────────────────────────────────────────────────────┐
│                   学籍番号解析フロー                         │
│                                                             │
│  入力: 1Y23F158-5                                          │
│        │                                                   │
│        ▼                                                   │
│  ┌─────────────────────┐                                   │
│  │ 1. 形式検証         │                                   │
│  │   - 正規表現マッチ   │                                   │
│  │   - 1[WXY]\d{2}[A-F] │                                   │
│  └──────────┬──────────┘                                   │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                   │
│  │ 2. 学部判定         │                                   │
│  │   - 1W → 基幹理工   │                                   │
│  │   - 1X → 創造理工   │                                   │
│  │   - 1Y → 先進理工   │                                   │
│  └──────────┬──────────┘                                   │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                   │
│  │ 3. 入学年度判定     │                                   │
│  │   - 23 → 2023年     │                                   │
│  └──────────┬──────────┘                                   │
│             │                                               │
│             ▼                                               │
│  ┌─────────────────────┐                                   │
│  │ 4. 学科/学系判定    │                                   │
│  │   - 基幹: 手動選択   │                                   │
│  │   - 創造/先進: 自動  │                                   │
│  └──────────┬──────────┘                                   │
│             │                                               │
│             ▼                                               │
│  出力:                                                      │
│  {                                                         │
│    faculty: "先進理工学部",                                │
│    enrollment_year: 2023,                                  │
│    department: "電気・情報生命工学科",                     │
│    needs_department_selection: false                       │
│  }                                                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 12. メール送信機能

### 12.1 メール種別

| 種別 | トリガー | 送信先 | 内容 |
|------|----------|--------|------|
| application_confirmation | 合宿申し込み完了 | 申し込み者 | 申し込み内容の確認 |
| department_required | 新学期処理後 | 基幹新2年生 | 学科選択の促し |
| change_request_notification | 変更リクエスト | 管理者 | 変更内容の通知 |
| enrollment_application | 入会申請 | 管理者 | 新規入会申請の通知 |

### 12.2 SMTP設定

```php
// config/mail.php
return [
    'smtp_host' => 'smtp.onamae.com',
    'smtp_port' => 587,
    'smtp_user' => 'noreply@example.com',
    'smtp_pass' => '********',
    'from_address' => 'noreply@example.com',
    'from_name' => '○○サークル 会計システム',
];
```

### 12.3 メールテンプレート例

**申し込み完了メール**
```
件名: 【○○サークル】2025年度春合宿 申し込み完了のお知らせ

{name} 様

2025年度春合宿への申し込みを受け付けました。

■ 申し込み内容
合宿名:     2025年度春合宿
日程:       2025年3月20日〜23日（3泊4日）
参加期間:   {join_day}日目 {join_timing} 〜 {leave_day}日目 {leave_timing}
往路バス:   {outbound_bus}
復路バス:   {return_bus}

■ 変更・キャンセルについて
申し込み後の変更・キャンセルは、幹事に連絡してください。
管理者の承認後に変更が反映されます。

---
このメールは自動送信されています。
返信はできませんのでご了承ください。
```
